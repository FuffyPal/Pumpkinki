[package]
name = "Pumpkinki"
version = "0.2.0"
edition = "2024"

[dependencies]
dotenv = "0.15.0"

[profile.release]
opt-level = 3
codegen-units = 1
panic = "abort"
lto = true

[profile.dev]
opt-level = 0
panic = "unwind"
